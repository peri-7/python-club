{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo \"no install step - handled in buildCommand\"",
  "buildCommand": "python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements-docs.txt && .venv/bin/python -m mkdocs build",
  "outputDirectory": "site"
}
