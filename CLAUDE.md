# CLAUDE.md

Οδηγίες για το project. Διάβασέ το πριν κάνεις αλλαγές.

## Τι είναι αυτό

Ιστοσελίδα με το υλικό ενός ομίλου Python για μαθητές λυκείου. Φτιαγμένη με
**MkDocs + Material**. Το περιεχόμενο είναι **στα ελληνικά**.

Οι μαθητές την ανοίγουν **μέσα στην τάξη**, δίπλα-δίπλα με τον editor τους.
Έχουν internet και εγκατεστημένη Python στα μηχανήματά τους.

Ο στόχος δεν είναι «οι φωτοτυπίες σε οθόνη». Είναι τα πράγματα που το χαρτί
**δεν** μπορεί να κάνει: αναζήτηση, αντιγραφή κώδικα, σύνδεσμοι προς την
επίσημη τεκμηρίωση, άμεσες διορθώσεις, κρυφές λύσεις.

## Περιβάλλον

```bash
conda activate python-club        # Python 3.12, mkdocs-material 9.7.7
mkdocs serve                      # http://127.0.0.1:8000, auto-reload
mkdocs build --strict             # πρέπει να περνάει πριν από κάθε commit
```

Αν το `conda` δεν είναι στο PATH: `C:\Users\peric\miniconda3\Scripts\conda.exe`,
και η python του env: `C:\Users\peric\miniconda3\envs\python-club\python.exe`.

## Δομή

```
mkdocs.yml                  ρυθμίσεις + nav (η πλοήγηση ορίζεται ΜΟΝΟ εδώ)
docs/                       το περιεχόμενο
├── index.md                ανακατεύθυνση στους Αρχάριους — δεν είναι καρτέλα
├── stylesheets/extra.css   χρώματα/τυπογραφία από τα έντυπα φυλλάδια
├── beginners/              ΚΑΡΤΕΛΑ 1
│   ├── index.md            τίτλος + πίνακας «Μαθήματα»
│   ├── theory/lesson-01.md + lesson-01.pdf
│   └── exercises/lesson-01.md + lesson-01.pdf
└── advanced/               ΚΑΡΤΕΛΑ 2 — ίδια δομή
site/                       ΠΑΡΑΓΟΜΕΝΟ. Μην το επεξεργάζεσαι, μην το κάνεις commit.
αρχαριοι/ προχωρημενοι/     ΤΟ ΠΡΩΤΟΤΥΠΟ ΥΛΙΚΟ (PDF + ένα HTML handout).
                            Πηγή αλήθειας για μετατροπές. Μην τα πειράζεις.
```

## Κανόνες που ΔΕΝ αλλάζουν χωρίς να ρωτήσεις

1. **Ο κώδικας είναι πάντα κείμενο, ποτέ screenshot.** Το αρχικό handout είχε 20
   εικόνες PNG με κώδικα. Μετατράπηκαν σε πραγματικά ```python μπλοκ. Screenshot
   κώδικα σημαίνει: δεν βρίσκεται στην αναζήτηση, δεν αντιγράφεται, και τα
   «έξυπνα» εισαγωγικά σπάνε με `SyntaxError`. Αυτός είναι ο λόγος ύπαρξης του site.
2. **Κάθε PDF κάθεται δίπλα στη σελίδα του, με το ίδιο όνομα.** Ο σύνδεσμος μέσα
   στη σελίδα είναι απλά `lesson-01.pdf`. Χωρίς `../../pdf/...`.
3. **Οι ετικέτες στο nav είναι σύντομες** (`Μάθημα 1`). Οι μεγάλοι τίτλοι
   αναδιπλώνονταν σε 3 γραμμές στην πλαϊνή στήλη. Ο πλήρης τίτλος είναι ο `# H1`
   της σελίδας και μπαίνει στον πίνακα του `index.md` του τμήματος.
4. **Τα «Θεωρία» / «Ασκήσεις» είναι ετικέτες ομάδας, χωρίς δική τους σελίδα.**
   Είχαν index σελίδες· ήταν διπλοί σύνδεσμοι προς το ίδιο πράγμα και διαγράφηκαν.
5. **Σύνδεσμοι προς docs.python.org αντί για επανάληψη της τεκμηρίωσης.** Μοτίβο:
   ένα `??? tip "Ψάξε μόνος σου"` στο τέλος κάθε ενότητας. Το να μάθουν να
   διαβάζουν την επίσημη τεκμηρίωση είναι σκοπός του μαθήματος, όχι πάρεργο.
6. **Οι λύσεις είναι κρυφές:** `??? success "Λύση"`.

## Προσθήκη νέου μαθήματος

1. `docs/<τμήμα>/theory/lesson-0N.md` + δίπλα το `lesson-0N.pdf`.
2. Το ίδιο στο `exercises/`.
3. Μία γραμμή στο `nav:` του `mkdocs.yml`, κάτω από τη σωστή ομάδα.
4. Μία γραμμή στον πίνακα του `docs/<τμήμα>/index.md`.
5. `mkdocs build --strict` — πρέπει να περνάει.

## Γνωστά σημεία προσοχής

- **Το αρχείο λέγεται `requirements-docs.txt`, ΟΧΙ `requirements.txt` — μη το
  μετονομάσεις.** Αν υπάρχει `requirements.txt` στη ρίζα, το Vercel νομίζει ότι
  το project είναι εφαρμογή Python, ψάχνει για entrypoint (`app.py`, `wsgi.py`
  κλπ) και το build αποτυγχάνει με «No python entrypoint found». Το ίδιο ισχύει
  για `pyproject.toml`, `Pipfile`, `setup.py` στη ρίζα.

- **Δεν υπάρχει ελληνικός stemmer** στη lunr. Το `search: lang: en` είναι
  σκόπιμο. Τα ελληνικά ευρετηριάζονται κανονικά (επιβεβαιωμένο), αλλά το
  «μεταβλητή» δεν ταυτίζεται αυτόματα με «μεταβλητές».
- **`not_in_nav: index.md`** χρειάζεται, αλλιώς το `--strict` κοκκινίζει επειδή
  το `docs/index.md` (η ανακατεύθυνση) δεν είναι στο nav.
- **Το φωτεινό θέμα είναι προεπιλογή σκόπιμα** — το `palette` δεν έχει `media:`.
  Μην το ξαναβάλεις: το σκοτεινό ήταν λιγότερο ευανάγνωστο στην τάξη.
- **Απόκρυψη δεξιού πίνακα περιεχομένων:** `hide: [toc]` στο front matter. Μπαίνει
  μόνο στα `index.md` των τμημάτων· τα μαθήματα κρατάνε τον δικό τους.
- Η ομάδα του Material προειδοποιεί ότι το **MkDocs 2.0** θα σπάσει όλα τα
  plugins. Μην αναβαθμίσεις στη μέση της σχολικής χρονιάς. Το περιεχόμενο είναι
  απλό Markdown, άρα μεταφέρσιμο αλλού αν χρειαστεί.

## Ύφος

Γράψε στα ελληνικά, στο ύφος των υπαρχόντων φυλλαδίων: σύντομες προτάσεις,
πληθυντικός πρώτου προσώπου («γράφουμε», «μπορούμε»), αγγλικός όρος σε παρένθεση
την πρώτη φορά — π.χ. «Μεταβλητές (variables)». Οι μαθητές είναι έφηβοι· μη
γράφεις υπεραπλουστευμένα ούτε με ακαδημαϊκό ύφος.
